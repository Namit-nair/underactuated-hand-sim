# Hardware-validation rig (single tendon-driven finger).
# Install into ONE environment that can also see the repo's config.py /
# analytical_model.py (run from the underactuated_finger/ folder, or add it to
# PYTHONPATH). mujoco is needed only to extract the moment arms from the
# high-fidelity model (same source of truth as validation.py); without it the
# dashboard falls back to the constant config.SHEATH_MOMENT_ARM.

pyrealsense2>=2.54        # Intel RealSense color stream
opencv-contrib-python>=4.7  # cv2.aruco (DICT_4X4_50, subpixel corner refine)
dynamixel-sdk>=3.8.4      # Dynamixel XM430-W350-T/R control (Protocol 2.0)
pyserial>=3.5            # Futek LCM300 / USB220 ASCII serial stream (load-test rig)
PySide6>=6.5              # dashboard GUI
matplotlib>=3.7           # embedded live plots
numpy>=1.23
mujoco>=3.0              # moment-arm extraction (optional but recommended)
